/* === Brand gradient + fonts === */
@import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;600;700&family=DM+Sans:wght@400;500;700&display=swap');

:root{
  --brand-bg: #05070b;
  --brand-fg: #e5e7eb;
  --brand-grad: linear-gradient(-45deg,#ff4d4d,#ff9a3c,#ffd23f,#34d399,#60a5fa,#a78bfa,#f472b6,#fb7185);
}

/* Animated gradient */
.html-gradient{
  background: var(--brand-grad);
  background-size: 400% 400%;
  animation: sweep 20s ease infinite;
}

/* Text gradient utility */
.brand-title{
  background: var(--brand-grad);
  background-size: 400% 400%;
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
  animation: sweep 20s ease infinite;
}

.font-brand{ font-family: 'Space_Grotesk','Space Grotesk',system-ui,ui-sans-serif; }
.font-ui{ font-family: 'DM Sans',system-ui,ui-sans-serif; }

@keyframes sweep{
  0%{ background-position: 0% 50% }
  50%{ background-position: 100% 50% }
  100%{ background-position: 0% 50% }
}
