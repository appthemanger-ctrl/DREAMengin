import Link from 'next/link';import { redirect } from 'next/navigation';import { supaServer } from '@/lib/supabase/server';import { widgetModules } from '@/lib/modules/registry.gen';export const dynamic='force-dynamic';type FeedItem={id:string;title?:string|null;summary?:string|null;url?:string|null;timestamp?:string|null;source?:string|null};async function getData(){const supa=supaServer();const { data:{ user }}=await supa.auth.getUser();if(!user) return {user:null as any,feed:[] as FeedItem[],widgets:[] as any[]};const { data:feedRaw }=await supa.from('feed_items').select('id,title,summary,url,timestamp,source').eq('user_id',user.id).order('timestamp',{ascending:false}).limit(30);const { data:layout }=await supa.from('dashboard_layout').select('widgets_json').eq('user_id',user.id).maybeSingle();const feed:Array<FeedItem>=Array.isArray(feedRaw)?feedRaw:[];const widgets:any[]=Array.isArray(layout?.widgets_json)?layout.widgets_json:[];return {user,feed,widgets}}export default async function HomePage(){const { user, feed, widgets }=await getData();if(!user) redirect('/login');const modules=Array.isArray(widgetModules)?widgetModules:[];const resolveMod=(t:string)=>modules.find((m:any)=>m.slug===t||m.name===t);return(<div className="relative min-h-[calc(100vh-4rem)]"><div className="pointer-events-none fixed inset-0 -z-10"><div aria-hidden="true" className="animated-spectrum" style={{position:'absolute',inset:0,background:'radial-gradient(1200px 600px at 10% 10%, rgba(99,102,241,0.25), transparent), radial-gradient(1200px 600px at 90% 20%, rgba(236,72,153,0.25), transparent), radial-gradient(1200px 600px at 50% 90%, rgba(16,185,129,0.25), transparent)'}}/></div><div className="relative mx-auto max-w-6xl px-4 py-6 grid grid-cols-1 md:grid-cols-3 gap-6"><section className="md:col-span-2 space-y-3"><div className="flex items-center justify-between"><h1 className="text-2xl font-semibold">Your Feed</h1><div className="flex gap-2"><Link href="/feed-settings" className="inline-flex items-center rounded-md border border-white/10 bg-white/5 px-3 py-1.5 text-sm hover:bg-white/10">Feed Rules</Link><Link href="/connectors" className="inline-flex items-center rounded-md border border-white/10 bg-white/5 px-3 py-1.5 text-sm hover:bg-white/10">Add Sources</Link></div></div>{feed.length>0?feed.map((it)=>(<article key={it.id} className="card p-4"><div className="text-xs opacity-70 mb-1">{(it.source??'source')} • {it.timestamp?new Date(it.timestamp).toLocaleString():''}</div><a href={it.url??'#'} target="_blank" className="block font-medium hover:underline">{it.title??'Untitled'}</a>{it.summary&&<p className="mt-1 text-sm opacity-90">{it.summary}</p>}</article>)):(<div className="card p-6 text-center"><div className="font-medium mb-2">No items yet</div><p className="text-sm mb-4 opacity-90">Connect sources and follow friends to populate your feed.</p><div className="flex items-center justify-center gap-3"><Link className="inline-flex items-center rounded-md border border-white/10 bg-white/5 px-3 py-1.5 text-sm hover:bg-white/10" href="/connectors">Add Sources</Link><Link className="inline-flex items-center rounded-md border border-white/10 bg-white/5 px-3 py-1.5 text-sm hover:bg-white/10" href="/discover">Discover</Link></div></div>)}</section><aside className="space-y-3"><div className="card p-4"><div className="font-medium mb-2">Widgets</div>{(widgets.length?widgets:[{id:'promo-1',type:'promo',enabled:true}]).map((w:any)=>{const mod:any=resolveMod(w.type);const Comp=mod?.Component;return(<div key={w.id??w.type} className="mb-3"><div className="text-sm opacity-70 mb-1">{mod?.name??w.type}</div>{Comp?(<Comp config={w.config??{}}/>):(<div className="rounded border border-white/10 p-3 text-sm">Widget “{w.type}” not installed. <Link href="/settings" className="underline">Install</Link></div>)}</div>)} )}<div className="mt-3 flex gap-2"><Link className="inline-flex items-center rounded-md border border-white/10 bg-white/5 px-3 py-1.5 text-xs hover:bg-white/10" href="/settings">Customize</Link><Link className="inline-flex items-center rounded-md border border-white/10 bg-white/5 px-3 py-1.5 text-xs hover:bg-white/10" href="/messages">Messages</Link></div></div></aside></div></div>)}
