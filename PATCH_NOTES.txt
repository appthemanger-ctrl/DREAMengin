NO-DELETES HOTFIX PATCH
-----------------------
This patch only ADDS missing files and fixes the @/* alias. It doesn't remove any of your files.

Added:
  - tsconfig.json (ensures @/* alias resolves to project root)
  - components/AudioPlayer.tsx (simple audio player)
  - components/DraggableModules.tsx (stub to satisfy imports)
  - lib/supabase/client.ts (browser Supabase client using env vars)
  - app/home/add/page.tsx (form to add custom feed item)
  - app/music/upload/page.tsx (form to add music release)

Notes:
  - Make sure your Vercel project has these env vars set:
      NEXT_PUBLIC_SUPABASE_URL
      NEXT_PUBLIC_SUPABASE_ANON_KEY
  - If your repo already has tsconfig.json, this one will REPLACE it to guarantee the @/* alias works.
  - If you prefer keeping your existing tsconfig.json, just copy the "baseUrl" and "paths" settings into yours.
