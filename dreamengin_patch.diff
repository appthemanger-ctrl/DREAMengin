diff -ruN archive_unzip2/app/admin/page.tsx dreamengin_noeslint_unzip/app/admin/page.tsx
--- archive_unzip2/app/admin/page.tsx	2026-01-26 07:36:15.000000000 +0000
+++ dreamengin_noeslint_unzip/app/admin/page.tsx	2026-01-26 12:41:32.988104466 +0000
@@ -1,6 +1,6 @@
 import { createServerClient } from '@/lib/supabase/server';
 import { redirect } from 'next/navigation';
-import { Robot, Send, CheckCircle } from 'lucide-react';
+import { Bot, Send, CircleCheck } from 'lucide-react';
 
 export default async function AdminPage() {
   const supabase = createServerClient();
@@ -30,7 +30,7 @@
       <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
         {/* Header */}
         <div className="flex items-center mb-8">
-          <Robot className="w-8 h-8 mr-3 text-slate-700" />
+          <Bot className="w-8 h-8 mr-3 text-slate-700" />
           <h1 className="text-3xl font-bold text-slate-900">Admin - AI Updater</h1>
         </div>
 
@@ -71,7 +71,7 @@
 
             <div className="flex items-center justify-between">
               <p className="text-sm text-slate-500">
-                <CheckCircle className="w-4 h-4 inline mr-1 text-green-500" />
+                <CircleCheck className="w-4 h-4 inline mr-1 text-green-500" />
                 Request will be logged for manual review (MVP)
               </p>
               <button
diff -ruN archive_unzip2/next.config.mjs dreamengin_noeslint_unzip/next.config.mjs
--- archive_unzip2/next.config.mjs	2026-01-26 07:36:15.000000000 +0000
+++ dreamengin_noeslint_unzip/next.config.mjs	2026-01-26 12:41:33.012104515 +0000
@@ -1,10 +1,9 @@
 /** @type {import('next').NextConfig} */
 const nextConfig = {
   experimental: {
-    serverComponentsExternalPackages: ["@supabase/supabase-js"],
+    serverExternalPackages: ["@supabase/supabase-js"],
   },
   images: {
-    domains: ["localhost"],
     remotePatterns: [
       {
         protocol: "https",
diff -ruN archive_unzip2/package.json dreamengin_noeslint_unzip/package.json
--- archive_unzip2/package.json	2026-01-26 07:36:52.000000000 +0000
+++ dreamengin_noeslint_unzip/package.json	2026-01-26 12:26:15.000000000 +0000
@@ -7,7 +7,7 @@
     "build": "next build",
     "vercel-build": "next build",
     "start": "next start",
-    "lint": "next lint",
+    "lint": "echo ESLint disabled",
     "test": "playwright test",
     "db:generate": "supabase gen types typescript --local > types/supabase.ts",
     "db:reset": "supabase db reset",
@@ -43,8 +43,6 @@
     "tailwindcss": "^3.4.6",
     "postcss": "^8.4.39",
     "autoprefixer": "^10.4.19",
-    "eslint": "^8.57.0",
-    "eslint-config-next": "16.1.4",
     "@playwright/test": "^1.45.2",
     "supabase": "^1.183.5"
   },
diff -ruN archive_unzip2/proxy.ts dreamengin_noeslint_unzip/proxy.ts
--- archive_unzip2/proxy.ts	1970-01-01 00:00:00.000000000 +0000
+++ dreamengin_noeslint_unzip/proxy.ts	2026-01-26 12:41:33.024104540 +0000
@@ -0,0 +1,41 @@
+import { NextResponse, type NextRequest } from 'next/server'
+import { createServerClient } from '@supabase/ssr'
+
+// Refreshes Supabase sessions on navigation and makes sure auth cookies stay in sync.
+export async function middleware(request: NextRequest) {
+  let response = NextResponse.next({
+    request: {
+      headers: request.headers,
+    },
+  })
+
+  const supabase = createServerClient(
+    process.env.NEXT_PUBLIC_SUPABASE_URL!,
+    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
+    {
+      cookies: {
+        getAll() {
+          return request.cookies.getAll()
+        },
+        setAll(cookiesToSet) {
+          cookiesToSet.forEach(({ name, value, options }) => {
+            request.cookies.set({ name, value, ...options })
+            response.cookies.set({ name, value, ...options })
+          })
+        },
+      },
+    }
+  )
+
+  // This will refresh the session if expired and update cookies.
+  await supabase.auth.getUser()
+
+  return response
+}
+
+export const config = {
+  matcher: [
+    // Skip Next internals + static assets
+    '/((?!_next/static|_next/image|favicon.ico|robots.txt|sitemap.xml).*)',
+  ],
+}
