name: readme-autosync
required_secrets: []
command: |
  [ -f scripts/readme-autosync.mjs ] && node scripts/readme-autosync.mjs || echo "⚠️ readme-autosync no-op"
artifact:
  name: readme-autosync-report
  path: ''
constraints:
  max_runtime_minutes: 10
  allow_failure: true
