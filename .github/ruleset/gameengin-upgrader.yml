name: gameengin-upgrader
required_secrets: [OPENAI_API_KEY]
command: |
  echo "gameengin-upgrader: ruleset placeholder — no-op" || true
artifact:
  name: ''
  path: ''
constraints:
  max_runtime_minutes: 20
  allow_failure: true
