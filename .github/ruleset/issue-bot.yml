name: issue-bot
required_secrets: []
command: |
  echo "issue-bot: handled by GitHub Script steps — no-op" || true
artifact:
  name: ''
  path: ''
constraints:
  max_runtime_minutes: 5
  allow_failure: true
