name: gameengin-artisan
required_secrets: [OPENAI_API_KEY]
command: |
  echo "gameengin-artisan: ruleset placeholder — no-op" || true
artifact:
  name: ''
  path: ''
constraints:
  max_runtime_minutes: 20
  allow_failure: true
