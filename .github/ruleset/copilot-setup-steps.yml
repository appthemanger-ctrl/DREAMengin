name: copilot-setup-steps
required_secrets: []
command: |
  echo "copilot-setup-steps: environment preparation — no-op at command level" || true
artifact:
  name: ''
  path: ''
constraints:
  max_runtime_minutes: 10
  allow_failure: true
