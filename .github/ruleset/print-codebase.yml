name: print-codebase
required_secrets: []
command: |
  [ -f scripts/print-codebase.mjs ] && node scripts/print-codebase.mjs > /dev/null 2>&1 \
  || echo "⚠️ print-codebase no-op"
artifact:
  name: codebase-print
  path: exports/**
constraints:
  max_runtime_minutes: 10
  allow_failure: true
