name: bot-pr-automerge
required_secrets: []
command: |
  echo "bot-pr-automerge: handled via GitHub Script — no-op" || true
artifact:
  name: ''
  path: ''
constraints:
  max_runtime_minutes: 5
  allow_failure: true
