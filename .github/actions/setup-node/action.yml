name: Setup Node 24
description: Standard Node runtime for DREAMengin workflows

runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v4
      with:
        version: 10.30.0
    - uses: actions/setup-node@v6
      with:
        node-version: 24
        cache: pnpm
