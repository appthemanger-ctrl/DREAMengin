name: Setup Node 24
description: Standard Node runtime for DREAMengin workflows

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v6
      with:
        node-version: 24
        cache: pnpm
