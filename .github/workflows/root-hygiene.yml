name: Root Hygiene

on:
  workflow_dispatch:

jobs:
  check-root:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Check root directory hygiene
        run: .github/scripts/check-root-hygiene.sh
